{
  "name": "react-modules",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/cray && yarn build"
  },
  "devDependencies": {
    "flow-bin": "^0.79.1"
  }
}
